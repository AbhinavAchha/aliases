# alias to be run with sudo only

alias _='sudo'
alias chown='sudo chown --changes'
alias make='make -j6'
alias smi='make install -j6'
alias su='sudo su'
alias build-neovim='sudo make CMAKE_BUILD_TYPE=RelWithDebInfo;sudo make install'
